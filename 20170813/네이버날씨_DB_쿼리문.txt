CREATE TABLE `naverweather` (
   no INT(20) NOT NULL AUTO_INCREMENT,
   title VARCHAR(200) NULL DEFAULT NULL,
   content VARCHAR(200) NULL DEFAULT NULL,
   producer VARCHAR(200) NULL DEFAULT NULL,
   date VARCHAR(200) NULL DEFAULT NULL,
   PRIMARY KEY (no)
);

